import './App.css';

const App = () => {
  const schedule = {
    title: "CS Courses for 2025-2026"
  };

  return (
    <div>
      <h1>{ schedule.title }</h1>
    </div>
  )
};

export default App;
